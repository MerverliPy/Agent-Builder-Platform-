# Custom Agent Builder Platform

Phase 0 scaffold: server boot and client app boot with health check connectivity.

Run instructions:
- Server: cd server && npm install && npm start
- Client: cd client && npm install && npm start
