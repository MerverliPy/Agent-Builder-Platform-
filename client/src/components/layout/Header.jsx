import React from 'react'
import { TopNavbar } from '../navigation/TopNavbar'

/**
 * Header Component
 * 
 * Main navigation header using the new product-grade TopNavbar.
 * This component wraps the new navigation system for backward compatibility.
 */

export const Header = () => {
  return <TopNavbar />
}

export default Header
